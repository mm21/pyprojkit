[project]
authors = [
  {name = "mm21", email = "mm21.dev@gmail.com"}
]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Natural Language :: English",
  "Operating System :: OS Independent",
  "Programming Language :: Python :: 3",  # managed by pyprojkit
  "Programming Language :: Python :: 3.12",  # managed by pyprojkit
  "Programming Language :: Python :: 3.13",  # managed by pyprojkit
  "Programming Language :: Python :: 3.14",  # managed by pyprojkit
  "Programming Language :: Python :: Implementation :: CPython",
  "Topic :: Software Development :: Libraries :: Python Modules",
  "Typing :: Typed"
]
dependencies = [
  "doit>=0.36.0,<0.37",
  "nox>=2026.4.10",
  "python-dotenv>=1.0.1,<2",
  "toml-sort>=0.24.2,<0.25",
  "tomlkit>=0.13.3,<0.14"
]
description = "Development workflow toolkit for Python projects: pyproject.toml sync, doit tasks, nox sessions"
license = "MIT"
license-files = ["LICENSE"]
name = "pyprojkit"
readme = "README.md"
requires-python = ">=3.12,<3.15"  # managed by pyprojkit
version = "0.3.1"

[project.optional-dependencies]
all = [
  "pyprojkit[analysis,doc,format,test]"
]
analysis = [
  "mypy[reports]>=1.5.1,<2",
  "pyright>=1.1.329,<2"
]
doc = [
  "mkinit"
]
format = [
  "autoflake>=2.3.1,<3",
  "black>=25.1.0,<26",
  "docfmt>=0.2.0,<0.3.0",
  "isort>=6.0.1,<7"
]
test = [
  "genbadge[tests,coverage]>=1.1.0,<2",
  "pytest>=8.4.2,<9",
  "pytest-cov>=7.1.0"
]

[project.scripts]
pyprojkit = "pyprojkit.cli:main"

[project.urls]
homepage = "https://github.com/mm21/pyprojkit"

[dependency-groups]
dev = [
  "pyprojkit[all]"
]

[build-system]
build-backend = "uv_build"
requires = ["uv_build>=0.11.6,<0.12.0"]

[tool.autoflake]  # managed by pyprojkit
in_place = true
recursive = true
remove-all-unused-imports = true
remove-unused-variables = true

[tool.black]  # managed by pyprojkit
quiet = true
target-version = [
  "py312",
  "py313",
  "py314"
]

[tool.coverage.report]  # managed by pyprojkit
exclude_lines = [
  "if TYPE_CHECKING:",
  "\\.\\.\\.$"
]

[tool.coverage.run]  # managed by pyprojkit
data_file = "__cache__/.coverage"

[tool.docfmt]  # managed by pyprojkit
force-reflow = true
in-place = true
recursive = true
summary-on-own-line = true

[tool.doit]  # managed by pyprojkit
dep_file = "__cache__/.doit.db"
verbosity = 2

[tool.isort]  # managed by pyprojkit
profile = "black"
quiet = true

[tool.nox]  # managed by pyprojkit
default_venv_backend = "uv"
envdir = "__cache__/nox"

[tool.pyprojkit]  # managed by pyprojkit
managed = [
  "tool.autoflake",
  "tool.black",
  "tool.coverage.report",
  "tool.coverage.run",
  "tool.docfmt",
  "tool.doit",
  "tool.isort",
  "tool.nox",
  "tool.pytest.ini_options",
  "tool.tomlsort"
]

[tool.pytest.ini_options]  # managed by pyprojkit
addopts = "--import-mode=importlib -s -v -rA"
cache_dir = "__cache__/pytest"
testpaths = "test"

[tool.tomlsort]  # managed by pyprojkit
sort_first = [
  "project",
  "dependency-groups",
  "build-system"
]
sort_table_keys = true
spaces_before_inline_comment = 2

[tool.uv.sources]
mkinit = {git = "https://github.com/mm21/mkinit.git"}
